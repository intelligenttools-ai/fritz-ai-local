[pytest]
pythonpath =
    hooks
    services/local-brain
testpaths =
    tests
    services/local-brain/tests
# Hard per-test timeout (pytest-timeout): turns a runaway hang into a failing
# test instead of an indefinitely-hung merge gate. 60s is far above any real
# unit test in this repo.
addopts = --timeout=60
